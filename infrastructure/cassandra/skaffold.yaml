apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: openframe-cassandra
build:
  artifacts:
    - image: ghcr.io/flamingo-cx/openframe-cassandra
      docker: {}
  local:
    useDockerCLI: false
    useBuildkit: false
    push: false
manifests:
  kustomize:
    paths:
    - ../../kind-cluster/apps/infrastructure/openframe-cassandra/manifests
