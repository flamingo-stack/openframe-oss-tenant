# Cross compilation configuration

[target.x86_64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y pkg-config libssl-dev",
]

[target.x86_64-pc-windows-gnu]
# Windows doesn't need OpenSSL, uses Windows native TLS

[target.x86_64-apple-darwin]
# Try using a Rust builder image that includes macOS SDK
# image = "joseluisq/rust-linux-darwin-builder:1.75.0"
# This image includes:
# - Rust toolchain
# - macOS SDK via osxcross
# - Ability to cross-compile to Darwin/macOS