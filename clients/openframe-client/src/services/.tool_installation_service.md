<!-- source-hash: da7c81a7bfcca8856b8eb57877ede2e5 -->
This service handles the complete lifecycle of tool installation, management, and cleanup for the Flamingo MSP platform's agent system.

## Key Components

- **ToolInstallationService** - Main service orchestrating tool downloads, installation, and lifecycle management
- **install()** - Primary method handling tool installation with asset downloads, command execution, and reinstall logic
- **Asset Management** - Downloads tools from GitHub, Artifactory, or Tool API with proper permissions
- **Directory Management** - Creates and manages tool-specific directories and executable paths
- **Process Management** - Handles stopping existing processes during reinstalls and cleanup

## Usage Example

```rust
use crate::services::ToolInstallationService;
use crate::models::ToolInstallationMessage;

// Initialize the service with all dependencies
let installation_service = ToolInstallationService::new(
    github_download_service,
    tool_agent_file_client,
    tool_api_client,
    command_params_resolver,
    url_params_resolver,
    installed_tools_service,
    directory_manager,
    tool_run_manager,
    tool_connection_processing_manager,
    config_service,
    installed_agent_publisher,
    tool_connection_service,
);

// Install a new tool
let tool_message = ToolInstallationMessage {
    tool_agent_id: "my-tool".to_string(),
    version: "1.0.0".to_string(),
    reinstall: false,
    // ... other fields
};

installation_service.install(tool_message).await?;
```

The service supports multiple asset sources (GitHub, Artifactory, Tool API), handles cross-platform executable permissions, manages reinstallation workflows, and integrates with the broader Flamingo agent ecosystem for MSP automation.