# Gateway service local environment configuration
spring:
  data:
    mongodb:
      uri: mongodb://openframe:password123456789@localhost:27017/openframe?authSource=admin&connectTimeoutMS=10000&socketTimeoutMS=10000&serverSelectionTimeoutMS=10000
    cassandra:
      contact-points: localhost
      port: 9042

jwt:
  issuer: openframe
  audience: openframe-web
  publicKey:
    key: classpath:certs/public.pem 