FROM mongo:7.0

# Install netcat (if needed, for debugging)
RUN apt-get update && apt-get install -y netcat && rm -rf /var/lib/apt/lists/*

# Copy initialization script
COPY scripts/init-mongo.js /docker-entrypoint-initdb.d/