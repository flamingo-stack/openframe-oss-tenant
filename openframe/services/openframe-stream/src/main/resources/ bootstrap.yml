spring:
  application:
    name: openframe-stream
  cloud:
    config:
      uri: http://openframe-config:8888
      fail-fast: true
      username: ${CONFIG_SERVER_USERNAME:config}
      password: ${CONFIG_SERVER_PASSWORD:secret} 