<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Include shared configuration from config server -->
    <include resource="optional:configserver:{spring.application.name}/shared-logback-spring.xml"/>
    
    <!-- Project-specific overrides can go here if needed -->
</configuration>