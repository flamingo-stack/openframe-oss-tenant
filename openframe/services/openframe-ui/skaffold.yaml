apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: openframe-ui
build:
  artifacts:
    - image: ghcr.io/flamingo-cx/openframe-oss-tenant/openframe-ui
      context: .
      sync: 
        manual:
          - src: "**/*"
            dest: "/app"
      docker:
        dockerfile: Dockerfile
      hooks:
        before:
          - command: [ "mvn", "clean", "package", "-DskipTests"]
  local:
    useDockerCLI: false
    useBuildkit: true
    push: false
manifests:
  helm:
    releases:
      - name: openframe-ui
        chartPath: ../../../manifests/microservices/openframe-ui
        valuesFiles:
          - ../../../manifests/microservices/openframe-ui/values.yaml
        # setValues: