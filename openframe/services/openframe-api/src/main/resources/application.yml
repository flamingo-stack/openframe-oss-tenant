spring:
  application:
    name: openframe-api
  config:
    import: configserver:${CONFIG_SERVER_URL:http://openframe-config-server:8888}
  profiles:
    group:
      local: local-config
---
spring:
  config:
    activate:
      on-profile: local-config
    import: "optional:configserver:http://localhost:8888"