apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: openframe-api
build:
  artifacts:
    - image: ghcr.io/flamingo-cx/openframe/openframe-api
      context: .
      sync: 
        manual:
          - src: "**/*"
            dest: "/app"
      docker:
        dockerfile: Dockerfile
      hooks:
        before:
          - command: [ "mvn", "clean", "package", "-DskipTests"]
  local:
    useDockerCLI: false
    useBuildkit: true
    push: false
manifests:
  helm:
    releases:
      - name: openframe-api
        chartPath: ../../../manifests/microservices/openframe-api
        valuesFiles:
          - ../../../manifests/microservices/openframe-api/values.yaml
        # setValues:
