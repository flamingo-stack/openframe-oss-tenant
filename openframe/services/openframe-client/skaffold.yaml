apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: openframe-client
build:
  artifacts:
    - image: ghcr.io/Flamingo-CX/openframe-oss-tenant/openframe-client
      context: .
      sync: 
        manual:
          - src: "**/*"
            dest: "/app"
      docker:
        dockerfile: Dockerfile
      hooks:
        before:
          - command: [ "mvn", "clean", "package", "-DskipTests"]
  local:
    useDockerCLI: false
    useBuildkit: true
    push: false
manifests:
  helm:
    releases:
      - name: openframe-client
        chartPath: ../../../manifests/microservices/openframe-client
        valuesFiles:
          - ../../../manifests/microservices/openframe-client/values.yaml
        # setValues: