# OpenFrame Frontend Environment Configuration

# Vite environment variables (React app)
VITE_APP_TYPE=openframe

# Backend API Configuration for K8s cluster
NEXT_PUBLIC_API_URL=https://localhost/api
VITE_API_URL=https://localhost/api
VITE_GRAPHQL_ENDPOINT=https://localhost/api/graphql
VITE_WS_ENDPOINT=wss://localhost/api/ws

# OAuth configuration - handled by Gateway
# No client credentials needed in frontend

# Development Configuration
NODE_ENV=development

# Platform Configuration
VITE_PLATFORM=openframe