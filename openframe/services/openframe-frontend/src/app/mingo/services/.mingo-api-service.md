<!-- source-hash: 05ac09c37d5f61921b03f356a50b1c9b -->
API service for Mingo chat operations, providing React Query mutations for dialog management, message handling, and approval workflows. Uses the centralized apiClient for HTTP requests and integrates toast notifications for error handling.

## Key Components

- **MingoApiService**: Static class containing all Mingo-specific API operations
- **createDialogMutation()**: Creates new ADMIN agent dialogs 
- **sendMessageMutation()**: Sends messages to existing dialogs
- **approveRequestMutation()**: Approves approval requests with toast error handling
- **rejectRequestMutation()**: Rejects approval requests with toast error handling
- **TypeScript Interfaces**: Type definitions for requests and responses (CreateDialogResponse, SendMessageRequest, etc.)

## Usage Example

```typescript
import { MingoApiService } from './mingo-api-service'

function ChatComponent() {
  // Create dialog mutation
  const createDialog = MingoApiService.createDialogMutation()
  
  // Send message mutation
  const sendMessage = MingoApiService.sendMessageMutation()
  
  // Approval mutations
  const approveRequest = MingoApiService.approveRequestMutation()
  const rejectRequest = MingoApiService.rejectRequestMutation()
  
  const handleCreateDialog = async () => {
    const dialog = await createDialog.mutateAsync()
    console.log('Created dialog:', dialog.id)
  }
  
  const handleSendMessage = async (dialogId: string, content: string) => {
    await sendMessage.mutateAsync({ dialogId, content })
  }
  
  const handleApproval = async (requestId: string, approve: boolean) => {
    if (approve) {
      await approveRequest.mutateAsync(requestId)
    } else {
      await rejectRequest.mutateAsync(requestId)
    }
  }
}
```