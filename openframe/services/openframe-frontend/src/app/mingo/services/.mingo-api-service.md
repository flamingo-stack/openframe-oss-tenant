<!-- source-hash: 05ac09c37d5f61921b03f356a50b1c9b -->
React Query service class for managing Mingo AI chat operations including dialog creation, message sending, and approval workflows.

## Key Components

### **MingoApiService Class**
Static service class that provides React Query mutations for:
- **`createDialogMutation()`** - Creates new admin chat dialogs
- **`sendMessageMutation()`** - Sends messages to existing dialogs
- **`approveRequestMutation()`** - Approves pending requests with toast notifications
- **`rejectRequestMutation()`** - Rejects pending requests with toast notifications

### **TypeScript Interfaces**
- **`CreateDialogResponse`** - Dialog creation response with id, status, and timestamps
- **`SendMessageResponse`** - Message sending response with messageId
- **`ApprovalResponse`** - Approval action response with status and timestamp
- **Request interfaces** for type-safe API calls

## Usage Example

```typescript
import { MingoApiService } from './mingo-api-service'

function ChatComponent() {
  // Create dialog mutation
  const createDialog = MingoApiService.createDialogMutation()
  
  // Send message mutation
  const sendMessage = MingoApiService.sendMessageMutation()
  
  // Approval mutations
  const approveRequest = MingoApiService.approveRequestMutation()
  const rejectRequest = MingoApiService.rejectRequestMutation()

  const handleCreateDialog = async () => {
    const dialog = await createDialog.mutateAsync()
    console.log('Created dialog:', dialog.id)
  }

  const handleSendMessage = async (dialogId: string) => {
    await sendMessage.mutateAsync({
      dialogId,
      content: 'Hello Mingo AI!'
    })
  }

  const handleApproval = async (requestId: string) => {
    await approveRequest.mutateAsync(requestId)
    // Toast notification handled automatically
  }

  return (
    <div>
      <button onClick={handleCreateDialog}>Create Dialog</button>
      {/* Chat UI components */}
    </div>
  )
}
```