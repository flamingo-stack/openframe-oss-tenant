<!-- source-hash: 05ac09c37d5f61921b03f356a50b1c9b -->
A service class providing React Query mutations for Mingo AI chat operations including dialog management, messaging, and approval workflows.

## Key Components

**Core Service Class:**
- `MingoApiService` - Static methods returning React Query mutations for API operations

**Type Definitions:**
- `CreateDialogResponse` - Dialog creation response with ID, status, and timestamps
- `SendMessageResponse` - Message sending confirmation with message ID
- `ApprovalResponse` - Approval request response with status and timestamp
- Request interfaces for dialog creation and message sending

**Mutation Methods:**
- `createDialogMutation()` - Creates new ADMIN agent dialogs
- `sendMessageMutation()` - Sends messages to existing dialogs
- `approveRequestMutation()` - Approves pending requests with error handling
- `rejectRequestMutation()` - Rejects pending requests with error handling

## Usage Example

```typescript
import { MingoApiService } from './mingo-api-service'

function ChatComponent() {
  const createDialog = MingoApiService.createDialogMutation()
  const sendMessage = MingoApiService.sendMessageMutation()
  const approveRequest = MingoApiService.approveRequestMutation()

  const handleCreateDialog = async () => {
    try {
      const dialog = await createDialog.mutateAsync()
      console.log('Dialog created:', dialog.id)
    } catch (error) {
      console.error('Failed to create dialog:', error)
    }
  }

  const handleSendMessage = async (dialogId: string, content: string) => {
    await sendMessage.mutateAsync({ dialogId, content })
  }

  const handleApproval = async (requestId: string) => {
    await approveRequest.mutateAsync(requestId)
  }

  return (
    <div>
      <button onClick={handleCreateDialog}>Start Chat</button>
      {/* Chat interface components */}
    </div>
  )
}
```