<!-- source-hash: 05ac09c37d5f61921b03f356a50b1c9b -->
A service class that provides React Query mutations for Mingo AI chat operations, including dialog creation, message sending, and approval management with built-in error handling and toast notifications.

## Key Components

**Interfaces:**
- `CreateDialogResponse` - Response structure for new dialog creation
- `SendMessageResponse` - Response structure for message sending
- `ApprovalResponse` - Response structure for approval/rejection operations
- `CreateDialogRequest` - Request payload for dialog creation
- `SendMessageRequest` - Request payload for message sending

**MingoApiService Class:**
- `createDialogMutation()` - Creates new admin dialogs
- `sendMessageMutation()` - Sends messages to existing dialogs
- `approveRequestMutation()` - Approves approval requests with toast feedback
- `rejectRequestMutation()` - Rejects approval requests with toast feedback

## Usage Example

```typescript
import { MingoApiService } from './mingo-api-service'

function ChatComponent() {
  const createDialog = MingoApiService.createDialogMutation()
  const sendMessage = MingoApiService.sendMessageMutation()
  const approveRequest = MingoApiService.approveRequestMutation()

  const handleCreateDialog = async () => {
    try {
      const dialog = await createDialog.mutateAsync()
      console.log('Created dialog:', dialog.id)
    } catch (error) {
      console.error('Failed to create dialog:', error)
    }
  }

  const handleSendMessage = async (dialogId: string, content: string) => {
    await sendMessage.mutateAsync({ dialogId, content })
  }

  const handleApproval = async (requestId: string) => {
    await approveRequest.mutateAsync(requestId)
    // Toast notification handled automatically
  }

  // Component JSX...
}
```