<!-- source-hash: acffb220d4ac9b144d95b5b0b3e1e8b2 -->
A Zustand store that manages message state for the Mingo AI chat interface, handling real-time conversations, streaming messages, and approval workflows across multiple dialog sessions.

## Key Components

### State Management
- **messagesByDialog** - Map storing messages for each dialog session
- **activeDialogId** - Currently selected dialog identifier
- **dialogs** - Array of available dialog nodes
- **streamingMessages** - Real-time message streaming state per dialog
- **segmentAccumulators** - Message segment processors for streaming content

### Real-time Features
- **typingStates** - Typing indicators per dialog
- **unreadCounts** - Unread message counters
- **Loading/Error States** - UI state management for async operations

### Core Actions
- **Message CRUD** - `setMessages`, `addMessage`, `updateMessage`, `removeMessage`
- **Streaming Support** - `setStreamingMessage`, `updateStreamingMessageSegments`
- **Approval Workflow** - `updateApprovalStatusInMessages`, approval request handling
- **Real-time Updates** - typing states, unread counters, segment accumulation

## Usage Example

```typescript
import { useMingoMessagesStore } from './stores/mingo-messages-store'

function ChatComponent() {
  const {
    activeDialogId,
    setActiveDialogId,
    getMessages,
    addMessage,
    setTyping,
    getOrCreateAccumulator
  } = useMingoMessagesStore()

  // Switch to a dialog
  const selectDialog = (dialogId: string) => {
    setActiveDialogId(dialogId)
    resetUnread(dialogId)
  }

  // Handle streaming message
  const handleStreamingSegments = (segments: MessageSegment[]) => {
    if (activeDialogId) {
      const accumulator = getOrCreateAccumulator(activeDialogId, {
        onApprove: (requestId) => handleApproval(requestId, 'approved'),
        onReject: (requestId) => handleApproval(requestId, 'rejected')
      })
      updateStreamingMessageSegments(activeDialogId, segments)
    }
  }

  // Get current messages
  const messages = activeDialogId ? getMessages(activeDialogId) : []
  
  return (
    <div>
      {messages.map(message => (
        <MessageComponent key={message.id} message={message} />
      ))}
    </div>
  )
}
```