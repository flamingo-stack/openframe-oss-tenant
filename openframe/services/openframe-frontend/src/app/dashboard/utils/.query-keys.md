<!-- source-hash: 9d3decbc752a6d8ac942c9113d19afd3 -->
Centralized query key factory for React Query cache management in the dashboard. This factory provides consistent, hierarchical query keys for all dashboard-related API calls and enables efficient cache invalidation.

## Key Components

- **`dashboardQueryKeys`** - Main factory object with methods for generating typed query keys
- **`all`** - Root key for all dashboard queries (`['dashboard']`)
- **`deviceStats()`** - Query key for device statistics data
- **`chatStats()`** - Query key for chat statistics (SaaS mode only)
- **`orgStats(limit)`** - Parameterized query key for organization statistics with limit
- **`invalidateAll()`** - Helper for invalidating all dashboard queries

## Usage Example

```typescript
import { useQuery, useQueryClient } from '@tanstack/react-query'
import { dashboardQueryKeys } from './query-keys'

// Use in React Query hooks
const { data: deviceStats } = useQuery({
  queryKey: dashboardQueryKeys.deviceStats(),
  queryFn: fetchDeviceStats
})

const { data: orgStats } = useQuery({
  queryKey: dashboardQueryKeys.orgStats(10),
  queryFn: () => fetchOrgStats(10)
})

// Invalidate all dashboard queries
const queryClient = useQueryClient()
queryClient.invalidateQueries({
  queryKey: dashboardQueryKeys.invalidateAll()
})
```

The hierarchical structure enables granular cache control - you can invalidate specific data types or all dashboard queries at once.