<!-- source-hash: 602a28f048fb0002ba6aacec5c77d128 -->
Barrel export file for the `DeviceSelector` component, re-exporting the component and its associated TypeScript types from their respective modules.

## Key Components

| Export | Type | Description |
|--------|------|-------------|
| `DeviceSelector` | Component | Main device selector UI component |
| `DeviceSelectorProps` | Type | Props interface for the `DeviceSelector` component |
| `InfiniteScrollConfig` | Type | Configuration interface for infinite scroll behavior |

## Usage Example

```typescript
import {
  DeviceSelector,
  type DeviceSelectorProps,
  type InfiniteScrollConfig,
} from '@/components/device-selector';

const scrollConfig: InfiniteScrollConfig = {
  pageSize: 20,
  threshold: 0.8,
};

const props: DeviceSelectorProps = {
  infiniteScrollConfig: scrollConfig,
  onDeviceSelect: (device) => console.log(device),
};
```