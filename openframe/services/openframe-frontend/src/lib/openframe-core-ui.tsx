'use client';

// Client boundary wrapper for `@flamingo-stack/openframe-frontend-core/components/ui`.
// This prevents Server Components from importing the core UI barrel directly (which uses hooks).
export * from '@flamingo-stack/openframe-frontend-core/components/ui';
