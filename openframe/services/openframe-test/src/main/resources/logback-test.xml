<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Silence noisy MongoDB driver logs -->
    <logger name="org.mongodb" level="WARN"/>
    <logger name="org.mongodb.driver" level="WARN"/>

    <!-- Silence other noisy libraries -->
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="org.selenide" level="INFO"/>
    <logger name="com.codeborne.selenide" level="INFO"/>

    <!-- Your application logs at INFO -->
    <logger name="com.openframe" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>