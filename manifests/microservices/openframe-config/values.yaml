registerJob:
  enabled: true
servicemonitor:
  enabled: true

image:
  repo: ghcr.io/Flamingo-CX/openframe-oss-tenant/openframe-config
  tag: latest

config:
  repo: "openframe-config"
  branch: "main"
