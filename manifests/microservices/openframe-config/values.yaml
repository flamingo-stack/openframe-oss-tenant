registerJob:
  enabled: true
servicemonitor:
  enabled: true

image:
  repo: ghcr.io/flamingo-stack/openframe-oss-tenant/openframe-config
  tag: latest

config:
  repo: "openframe-config"
  branch: "main"
