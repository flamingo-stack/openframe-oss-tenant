{{- if include "chart.localhost.hasTLS" . }}
apiVersion: v1
kind: Secret
metadata:
  name: localhost-tls
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.deployment.oss.ingress.localhost.tls.cert | b64enc }}
  tls.key: {{ .Values.deployment.oss.ingress.localhost.tls.key | b64enc }}
{{- end }}
