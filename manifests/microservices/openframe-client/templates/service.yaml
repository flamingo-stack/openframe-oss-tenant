apiVersion: v1
kind: Service
metadata:
  name: openframe-client
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app: openframe-client
spec:
  selector:
    app: openframe-client
  ports:
    - port: 8097
      targetPort: 8097
      name: http
    - port: 8102
      targetPort: 8102
      name: management
  type: ClusterIP
