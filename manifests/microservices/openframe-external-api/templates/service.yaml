apiVersion: v1
kind: Service
metadata:
  name: openframe-external-api
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app: openframe-external-api
spec:
  selector:
    app: openframe-external-api
  ports:
    - port: 8092
      targetPort: 8092
      name: http
    - port: 8093
      targetPort: 8093
      name: management
  type: ClusterIP
