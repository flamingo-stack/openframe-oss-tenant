apiVersion: v1
kind: Service
metadata:
  name: external-api
  labels:
    app: external-api
spec:
  selector:
    app: external-api
  ports:
    - port: 8092
      targetPort: 8092
      name: http
    - port: 8093
      targetPort: 8093
      name: management
  type: ClusterIP
