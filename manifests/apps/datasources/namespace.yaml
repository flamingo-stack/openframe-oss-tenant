apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: namespace-datasources
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default

  sources:
    - repoURL: https://github.com/Flamingo-CX/openframe.git
      targetRevision: feature/remove-ingresses
      path: manifests/datasources/base/namespace

  destination:
    name: in-cluster
    namespace: datasources

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: -1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 30m
