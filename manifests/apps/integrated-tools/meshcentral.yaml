apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: meshcentral
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: integrated-tools

  sources:
    - repoURL: https://github.com/Flamingo-CX/openframe.git
      targetRevision: debezium
      path: manifests/integrated-tools/base/meshcentral

  destination:
    name: in-cluster
    namespace: integrated-tools

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: -1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 30m
