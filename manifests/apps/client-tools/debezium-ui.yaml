apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: debezium-ui
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: client-tools

  sources:
    - repoURL: https://github.com/Flamingo-CX/openframe.git
      targetRevision: feature/dev-mode
      path: manifests/client-tools/base/debezium-ui

  destination:
    name: in-cluster
    namespace: client-tools

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: -1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 30m
