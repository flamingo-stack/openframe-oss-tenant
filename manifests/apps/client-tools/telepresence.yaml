apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: telepresence
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: client-tools

  sources:
    # Helm chart
    - repoURL: https://getambassador.io
      targetRevision: 2.20.3-test.0
      chart: traffic-manager
      helm:
        valueFiles:
          - "$values/manifests/client-tools/base/telepresence/helm-values.yaml"  # name is hardcoded
    - repoURL: https://github.com/Flamingo-CX/openframe.git
      targetRevision: feature-fix-ci
      ref: values
    # Extra resources
    - repoURL: https://github.com/Flamingo-CX/openframe.git
      targetRevision: feature-fix-ci
      path: manifests/client-tools/base/telepresence

  destination:
    name: in-cluster
    namespace: client-tools

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=false
    retry:
      limit: -1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 30m
