apiVersion: batch/v1
kind: Job
metadata:
  name: fleetmdm-mysql-init-privileges
  annotations:
    "argocd.argoproj.io/hook": PostSync
    "argocd.argoproj.io/hook-delete-policy": HookSucceeded
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: init-privileges
          image: mysql:8.0.35
          env:
            - name: MYSQL_USER
              valueFrom:
                configMapKeyRef:
                  name: fleetmdm-mysql
                  key: MYSQL_USER
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fleetmdm-mysql
                  key: MYSQL_ROOT_PASSWORD
          command:
            - /bin/sh
            - -c
            - |
              echo "Waiting for MySQL to be ready..."
              until mysql -h fleetmdm-mysql-0.fleetmdm-mysql.integrated-tools.svc.cluster.local -u root -p${MYSQL_ROOT_PASSWORD} -e "SELECT 1" 2>/dev/null; do
                echo "Still waiting for MySQL..."
                sleep 5
              done
              
              echo "Granting Debezium privileges to ${MYSQL_USER}..."
              mysql -h fleetmdm-mysql-0.fleetmdm-mysql.integrated-tools.svc.cluster.local -u root -p${MYSQL_ROOT_PASSWORD} -e "GRANT RELOAD, FLUSH_TABLES, REPLICATION CLIENT ON *.* TO '${MYSQL_USER}'@'%'; FLUSH PRIVILEGES;"
              
              echo "Verifying privileges..."
              mysql -h fleetmdm-mysql-0.fleetmdm-mysql.integrated-tools.svc.cluster.local -u root -p${MYSQL_ROOT_PASSWORD} -e "SHOW GRANTS FOR '${MYSQL_USER}'@'%';"
              
              echo "MySQL user privileges configured successfully."
