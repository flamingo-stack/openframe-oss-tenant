apiVersion: v1
kind: Service
metadata:
  name: fleetmdm-server
spec:
  clusterIP: None
  selector:
    app: fleetmdm-server
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
