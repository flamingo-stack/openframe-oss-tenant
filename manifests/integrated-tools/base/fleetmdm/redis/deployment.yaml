apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleetmdm-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fleetmdm-redis
  template:
    metadata:
      labels:
        app: fleetmdm-redis
    spec:
      imagePullSecrets: 
        - name: docker-pat-secret
    
      containers:
      - name: redis
        image: redis:latest
        ports:
        - containerPort: 6379
        volumeMounts:
        - name: redis-data
          mountPath: /data
        livenessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 30
          periodSeconds: 10
      volumes:
      - name: redis-data
        persistentVolumeClaim:
          claimName: fleetmdm-redis
