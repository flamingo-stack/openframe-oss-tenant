apiVersion: v1
kind: Service
metadata:
  name: debezium-connect
  namespace: openframe-datasources
spec:
  selector:
    app: debezium-connect
  ports:
    - port: 8083
      targetPort: 8083
      name: http