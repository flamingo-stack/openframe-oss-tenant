mongodb:
  uri: "mongodb://openframe:password123456789@mongodb:27017/openframe?authSource=admin"

# use below for storig uri with password
# existingSecret:
#   name: ""
#   key: "mongodb-uri"

serviceMonitor:
  enabled: true

imagePullSecrets:
  - name: docker-pat-secret