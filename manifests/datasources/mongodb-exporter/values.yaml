registerJob:
  enabled: true


prometheus-mongodb-exporter:
  mongodb:
    uri: "mongodb://openframe:password@mongodb:27017/openframe?authSource=admin"

  # use below for storig uri with password
  # existingSecret:
  #   name: ""
  #   key: "mongodb-uri"

  serviceMonitor:
    enabled: false
