apiVersion: v2
name: mongodb-exporter-wrapper
description: Wrapper chart for Prometheus Mongodb Exporter
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: prometheus-mongodb-exporter
    version: ">=3.12.0"
    repository: https://prometheus-community.github.io/helm-charts
