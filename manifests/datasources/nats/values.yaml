environment: dev

registerJob:
  enabled: true
  image:
    registry: ghcr.io/flamingo-stack/registry
    tag: "8.9.1"
servicemonitor:
  enabled: true

image:
  repository: ghcr.io/flamingo-stack/registry/library/nats
  tag: "2.10.10-alpine"

credentials:
  admin_user: "admin"            
  admin_pass: "admin_password"
  device_user: "machine"
  service_user: "service"
  service_pass: "service_pass"

