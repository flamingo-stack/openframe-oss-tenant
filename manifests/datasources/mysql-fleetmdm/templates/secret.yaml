apiVersion: v1
kind: Secret
metadata:
  name: fleetmdm-mysql
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "${FLEETMDM_MYSQL_ROOT_PASSWORD}"
  MYSQL_PASSWORD: "${FLEETMDM_MYSQL_PASSWORD}"
  MYSQL_DEFAULT_AUTHENTICATION_PLUGIN: "mysql_native_password"

