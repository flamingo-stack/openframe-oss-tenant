deployment:
  selfHosted:
    enabled: true    
    # Ingress configuration - enable only ONE of the following
    ingress:
      localhost:
        enabled: true   # Enable localhost ingress with nginx
      ngrok:
        enabled: false  # Enable ngrok ingress for external access
        credentials:
          apiKey: ""
          authtoken: ""
  saas:
    enabled: false
    ingress:
      localhost:
        enabled: true   # Always enabled for SaaS deployment

global:
  repoDir: manifests
  appsDir: apps
  repoURL: https://github.com/Flamingo-CX/openframe.git

  repoBranch: main
  autoSync: true

registry:
  docker:
    server: https://index.docker.io/v1/
    username: dmtkov
    password: dckr_pat_lNRkLN8XVjYltdefPwdLuMFjT9Y
    email: dmtkov@flamingo.cx
  ghcr:
    server: ghcr.io
    username: CI
    password: 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
    email: CI@flamingo.cx

registerJob:
  enabled: true

apps:
  openframe-config:
    values:
      config:
        branch: main
