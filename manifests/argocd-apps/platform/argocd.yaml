apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-ingress
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: platform

  sources:
    - repoURL: https://github.com/Flamingo-CX/openframe.git
<<<<<<< HEAD
      targetRevision: feature/ngrok
      path: manifests/platform/argocd
=======
      targetRevision: main
      path: manifests/platform/base/argocd
>>>>>>> 8245907e (back to main)

  destination:
    name: in-cluster
    namespace: argocd
    
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: -1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 30m
