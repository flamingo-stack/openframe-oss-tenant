apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
  
configMapGenerator:
  - name: platform
    files:
      - ./argocd.json
      - ./grafana.json
      - ./loki.json
      - ./prometheus.json
      - ./promtail.json

generatorOptions:
  disableNameSuffixHash: true
