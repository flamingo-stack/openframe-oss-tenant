apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# resources:
#   - grafana-provisioning.yaml
  
configMapGenerator:
  - name: platform
    files:
      - ./argocd.json
      - ./loki.json
      - ./prometheus.json
      - ./promtail.json

generatorOptions:
  disableNameSuffixHash: true
