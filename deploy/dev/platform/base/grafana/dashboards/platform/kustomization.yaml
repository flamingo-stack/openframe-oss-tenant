apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
  
configMapGenerator:
  - name: platform
    files:
      - ./grafana.json
      - ./loki.json
      - ./prometheus.json
      - ./promtail.json

generatorOptions:
  disableNameSuffixHash: true
