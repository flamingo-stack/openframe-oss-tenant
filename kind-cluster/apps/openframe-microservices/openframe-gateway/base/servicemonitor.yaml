apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openframe-gateway
spec:
  endpoints:
    - port: management
      path: /management/v1/prometheus
      interval: 30s
  namespaceSelector:
    matchNames:
    - infrastructure
  selector:
    matchLabels:
      app: openframe-gateway
