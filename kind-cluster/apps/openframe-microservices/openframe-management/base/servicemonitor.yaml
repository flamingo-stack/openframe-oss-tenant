apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openframe-management
spec:
  endpoints:
    - port: management
      path: /management/v1/prometheus
      interval: 30s
  namespaceSelector:
    matchNames:
    - openframe-microservices
  selector:
    matchLabels:
      app: openframe-management
