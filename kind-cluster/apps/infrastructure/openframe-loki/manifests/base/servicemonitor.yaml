apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openframe-loki
  labels:
    app: openframe-loki
spec:
  selector:
    matchLabels:
      app: openframe-loki
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
  namespaceSelector:
    # any: true
    matchNames:
      - monitoring
