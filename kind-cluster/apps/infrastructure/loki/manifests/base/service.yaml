apiVersion: v1
kind: Service
metadata:
  name: openframe-loki
  labels:
    app: openframe-loki
spec:
  ports:
    - name: http-metrics
      port: 3100
      targetPort: 3100
      protocol: TCP
  selector:
    app: openframe-loki
