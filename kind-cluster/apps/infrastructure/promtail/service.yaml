apiVersion: v1
kind: Service
metadata:
  name: promtail
  namespace: default
spec:
  ports:
    - name: http-metrics
      port: 9080
      targetPort: 9080
      protocol: TCP
  selector:
    app: promtail