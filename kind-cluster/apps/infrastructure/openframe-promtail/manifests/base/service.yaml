apiVersion: v1
kind: Service
metadata:
  name: openframe-promtail
  labels:
    app: openframe-promtail
spec:
  ports:
    - name: http-metrics
      port: 9080
      targetPort: 9080
      protocol: TCP
  selector:
    app: openframe-promtail