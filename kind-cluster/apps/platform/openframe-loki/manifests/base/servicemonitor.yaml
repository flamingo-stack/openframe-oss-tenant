apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openframe-loki
  labels:
    app: openframe-loki
spec:
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
  namespaceSelector:
    # any: true
    matchNames:
      - monitoring
  selector:
    matchLabels:
      app: openframe-loki
