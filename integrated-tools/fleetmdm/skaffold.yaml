apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: fleetmdm
build:
  artifacts:
    - image: ghcr.io/flamingo-cx/openframe/fleetmdm
      context: .
      sync: 
        manual:
          - src: "**/*"
            dest: "/app"
      docker:
        dockerfile: Dockerfile
      hooks:
        before:
          - command: [ "mvn", "clean", "package", "-DskipTests"]
  local:
    useDockerCLI: false
    useBuildkit: true
    push: false
manifests:
  kustomize:
    paths:
    - ../../manifests/integrated-tools/fleetmdm
